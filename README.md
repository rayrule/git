# D Git Workspace

This repository is the local Git workspace at `D:\git`.

## Structure

- Add project files directly here, or create subdirectories for separate parts of the project.
- Keep generated files, build outputs, temporary files, and secrets out of Git.

## Common Commands

```powershell
git status
git add .
git commit -m "Describe the change"
```
